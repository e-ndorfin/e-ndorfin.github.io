# Project conventions

- Use the shared theme colors defined in `tailwind.config.mjs`; avoid adding one-off colors.
- Links use burgundy by default and gold on hover.
- Bold text uses the same burgundy as links.
